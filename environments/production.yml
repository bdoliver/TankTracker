# configuration file for production environment

# only log warning and error messsages
log: "warning"

# log message to a file in logs/
logger: "file"

# don't consider warnings critical
warnings: 1

# hide errors 
show_errors: 1

# cache route resolution for maximum performance
route_cache: 1

# cookie path in production:
#session_cookie_path: "/TankTracker"

plack_middlewares:
    -
        - "+Dancer::Middleware::Rebase"
        - base
#        - "http://caboo.homelinux.net/TankTracker"
        - "http://localhost/TankTracker"
        - strip
        - 1

# db_dsn: "dbi:Pg:dbname=TankTracker;host=localhost;port=5432"
# db_user: "brendon"
# db_pass:
